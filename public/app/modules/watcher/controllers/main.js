define(function (require) {

    var angular = require('angular');
    var module = require('module!@');
    //var moment = require('moment');

    module.controller('watcher.controllers.main', [
        '$rootScope', '$scope', '$modal', '$state', '$stateParams',
        function ($rootScope, $scope, $modal, $state, $stateParams) {

        }
    ]);
});
